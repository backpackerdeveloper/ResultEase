'use client'

import React from 'react'

/**
 * Auth Layout
 * Minimal layout for auth pages
 * No header/footer - focuses on authentication
 */
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
